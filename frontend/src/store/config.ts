export enum StoreNames {
	AUTH = 'auth',
	USER = 'user',
	BOOK = 'book',
	TRANSACTION='transaction',
	OTHER='other'
}
export enum SELECTED_MENU{
	BOOK="book",
	MEMBER='member',
	BORROWED='borrowed',
	RETURNED='returned',
	TRANSACTION='transaction',
}
export enum TRANSACTION_TYPES {
	BORROWED = 'BORROWED',
	RETURNED = 'RETURNED',
}